#include "main"
/**
 * add - takes two number and add them together
 * @a: firs integer variable
 * @b: second integer variable
 *
 * Return: result
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}

