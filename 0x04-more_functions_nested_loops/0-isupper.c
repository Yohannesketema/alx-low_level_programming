#include <stdio.h>
#include "main.h"
/**
 * isupper is a function that verifies if a character is upeercase or not
 * returns one if its upper case
 */
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
		return (1);
}
/*
 * int main is the main function
 *
 * if its correct returns 0
 */
int main()
{ 
	_isupper(A);
		return (0);
}
